Mocha is a feature rich JS framework running on NodeJs and the browser for Unit testing. It also provides async testing in a simplified manner. 
It executes the tests serially and mapps the uncaught exceptions to the correct test cases. 
It supports PROMISE. 
npm install -g mocha